auto-install-peers=true
strict-peer-dependencies=false
# Expo / React Native, pnpm'in symlink'li yapısıyla uyumsuz (phantom dependency
# sorunları). Expo'nun pnpm monorepo önerisi: düz node_modules.
node-linker=hoisted
